# Source provenance

This directory is copied from `components/esp_lcd_co5300` in
<https://github.com/esp-mosaico/esp-mosaico-bsp> at commit
`bef99672e411101489ed19c40527cca1c1dd5bb1`.

The copied component reports version `2.2.0` and is licensed under Apache-2.0;
the upstream `license.txt` is retained unchanged. It is vendored here so the
ESP-Mosaico board integration does not depend on the BSP repository at build
time.
