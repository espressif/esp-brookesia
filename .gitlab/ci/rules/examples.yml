#
# Agent
#
# agent/chatbot
.rules:build:examples_agent_chatbot:
  rules:
    - !reference [.patterns-if-protected]
    - !reference [.patterns-if-label-build]
    - !reference [.patterns-if-label-target_test]
    - !reference [.patterns-if-trigger-job]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-build_system]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-examples_agent_chatbot]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_hal_adaptor]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_hal_boards]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_hal_interface]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_agent_coze]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_agent_manager]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_helper]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_agent_openai]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_agent_xiaozhi]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_expression_emote]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_audio]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_helper]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_manager]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_storage]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_sntp]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_wifi]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_mcp_utils]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_lib_utils]

#
# Service
#
# service/audio
.rules:build:examples_service_audio:
  rules:
    - !reference [.patterns-if-protected]
    - !reference [.patterns-if-label-build]
    - !reference [.patterns-if-label-target_test]
    - !reference [.patterns-if-trigger-job]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-build_system]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-examples_service_audio]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_hal_adaptor]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_hal_boards]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_hal_interface]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_audio]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_helper]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_manager]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_storage]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_lib_utils]
# service/console
.rules:build:examples_service_console:
  rules:
    - !reference [.patterns-if-protected]
    - !reference [.patterns-if-label-build]
    - !reference [.patterns-if-label-target_test]
    - !reference [.patterns-if-trigger-job]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-build_system]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-examples_service_console]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_hal_adaptor]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_hal_boards]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_hal_interface]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_agent_coze]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_agent_manager]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_helper]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_agent_openai]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_agent_xiaozhi]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_expression_emote]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_audio]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_helper]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_manager]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_storage]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_sntp]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_video]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_wifi]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_mcp_utils]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_lib_utils]
# service/http
.rules:build:examples_service_http:
  rules:
    - !reference [.patterns-if-protected]
    - !reference [.patterns-if-label-build]
    - !reference [.patterns-if-label-target_test]
    - !reference [.patterns-if-trigger-job]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-build_system]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-examples_service_http]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_hal_adaptor]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_hal_interface]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_device]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_http]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_wifi]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_helper]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_manager]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_lib_utils]
# service/storage
.rules:build:examples_service_storage:
  rules:
    - !reference [.patterns-if-protected]
    - !reference [.patterns-if-label-build]
    - !reference [.patterns-if-label-target_test]
    - !reference [.patterns-if-trigger-job]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-build_system]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-examples_service_storage]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_helper]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_manager]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_storage]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_lib_utils]
# service/wifi
.rules:build:examples_service_wifi:
  rules:
    - !reference [.patterns-if-protected]
    - !reference [.patterns-if-label-build]
    - !reference [.patterns-if-label-target_test]
    - !reference [.patterns-if-trigger-job]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-build_system]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-examples_service_wifi]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_helper]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_manager]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_storage]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_wifi]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_lib_utils]

#
# System
#
# system/super
.rules:build:examples_system_super:
  rules:
    - !reference [.patterns-if-protected]
    - !reference [.patterns-if-label-build]
    - !reference [.patterns-if-label-target_test]
    - !reference [.patterns-if-trigger-job]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-build_system]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-examples_system_super]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_hal_adaptor]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_hal_boards]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_hal_interface]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_gui_lvgl]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_runtime_js]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_runtime_manager]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_audio]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_device]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_display]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_helper]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_http]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_manager]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_emulation_nes]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_sntp]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_storage]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_video]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_service_wifi]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_system_core]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_system_super]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_app_settings]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_app_store]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_app_files]
    - if: !reference [.patterns-if-dev-push, if]
      changes: !reference [.patterns-component_brookesia_lib_utils]
