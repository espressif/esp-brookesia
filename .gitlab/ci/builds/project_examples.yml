#
# Agent
#
# agent/chatbot
build_examples_agent_chatbot:
  extends:
    - .build_template_without_large_artifacts
    - .build_idf_release_version_for_chatbot
    - .rules:build:examples_agent_chatbot
  variables:
    EXAMPLE_DIR: examples/agent/chatbot

#
# Service
#
# service/audio
build_examples_service_audio:
  extends:
    - .build_template_without_large_artifacts
    - .build_idf_release_version_with_board
    - .rules:build:examples_service_audio
  variables:
    EXAMPLE_DIR: examples/service/audio
# service/console
build_examples_service_console:
  extends:
    - .build_template_without_large_artifacts
    - .build_idf_release_version_with_board
    - .rules:build:examples_service_console
  variables:
    EXAMPLE_DIR: examples/service/console
# service/device
build_examples_service_device:
  extends:
    - .build_template_without_large_artifacts
    - .build_idf_release_version_with_board
    - .rules:build:examples_service_device
  variables:
    EXAMPLE_DIR: examples/service/device
# service/nvs
build_examples_service_nvs:
  extends:
    - .build_template
    - .build_idf_release_version_general
    - .rules:build:examples_service_nvs
  variables:
    EXAMPLE_DIR: examples/service/nvs
# service/wifi
build_examples_service_wifi:
  extends:
    - .build_template
    - .build_idf_release_version_general
    - .rules:build:examples_service_wifi
  variables:
    EXAMPLE_DIR: examples/service/wifi
