#
# Agent
#
# agent/chatbot
build_examples_agent_chatbot:
  extends:
    - .build_template_without_large_artifacts
    - .build_idf_release_version_for_chatbot
    - .rules:build:examples_agent_chatbot
  variables:
    EXAMPLE_DIR: examples/agent/chatbot

#
# Service
#
# service/audio
build_examples_service_audio:
  extends:
    - .build_template_without_large_artifacts
    - .build_idf_release_version_with_board
    - .rules:build:examples_service_audio
  variables:
    EXAMPLE_DIR: examples/service/audio
# service/console
build_examples_service_console:
  extends:
    - .build_template_without_large_artifacts
    - .build_idf_release_version_with_board
    - .rules:build:examples_service_console
  variables:
    EXAMPLE_DIR: examples/service/console
# service/http
build_examples_service_http:
  extends:
    - .build_template
    - .build_idf_release_version_general
    - .rules:build:examples_service_http
  variables:
    EXAMPLE_DIR: examples/service/http
# service/storage
build_examples_service_storage:
  extends:
    - .build_template
    - .build_idf_release_version_general
    - .rules:build:examples_service_storage
  variables:
    EXAMPLE_DIR: examples/service/storage
# service/wifi
build_examples_service_wifi:
  extends:
    - .build_template
    - .build_idf_release_version_general
    - .rules:build:examples_service_wifi
  variables:
    EXAMPLE_DIR: examples/service/wifi

#
# System
#
# system/super
build_examples_system_super:
  extends:
    - .build_template_without_large_artifacts
    - .build_idf_release_version_for_super
    - .rules:build:examples_system_super
  variables:
    EXAMPLE_DIR: examples/system/super
